<!DOCTYPE html>
<html lang="ar">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="application-name" content="{{=request.application}}">
    <meta name="google-site-verification" content="">
      
      
    <link rel="stylesheet" href="{{=URL('static','css/page.css')}}"/>
    <link rel="stylesheet" href="{{=URL('static','css/bootstrap.min.css')}}"/>
    
    <link rel="stylesheet" href="{{=URL('static','css/bootstrap-theme.min.css')}}"/>
      <link rel="stylesheet" href="{{=URL('static','css/web2py-bootstrap3.css')}}"/>
    <link rel="apple-touch-icon" href="{{=URL('static','images/waves.png')}}">
     <link rel="shortcut icon" href="{{=URL('static','images/waves.png')}}" type="image/png">
       <script src="{{=URL('static','js/modernizr-2.8.3.min.js')}}"></script>
     <title>{{=response.title or request.application}}</title>
 </head>
 <body>
      <div class="container-fluid">
     <nav class="collapse navbar-collapse">
         
        <ul class="nav nav-tabs nav-justified">
          <li role="presentation" class="active"><a href="{{=URL('default','index')}}">الرئيسية</a></li>
          <li role="presentation"><a href="{{=URL('default','add_daily')}}">العمليات اليومية</a></li>
          <li role="presentation"><a href="{{=URL('reports','index')}}">التقارير والمراجعه</a></li>
          <li role="presentation"><a href="{{=URL('default','addmanage')}}" >عمليات إداريه</a></li>
        </ul>
     </nav>
           <div class="flash alert alert-dismissable">{{=response.flash or ''}}</div>
     </div>
     
     <div class="container">
     
         
             
        
          {{block center}}
        {{include}}
        {{end}}
         
         
     
     </div>
     
     
     
     
     
     
     <footer class="footer">
         
         
         
     </footer>
     
     <script src="{{=URL('static','js/jquery.js')}}"></script>

     <script src="{{=URL('static','js/bootstrap.min.js')}}"></script>
     
   </body>
</html>
